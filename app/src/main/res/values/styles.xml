<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorOnPrimary">@color/onPrimaryColor</item>
        <item name="colorOnSecondary">@color/onSecondaryColor</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="AppTheme.Launcher" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_background</item>
        <item name="colorPrimaryDark">@color/primaryColor</item>
    </style>

    <style name="AppTheme.FABMenu.Description" parent="AppTheme">
        <item name="android:textColor">@color/onSecondaryColor</item>
        <item name="android:background">@drawable/fab_description</item>
        <item name="android:padding">4dp</item>
    </style>

</resources>
